// console.log("2" > 1); // in these kind of comparison result is not always predictable

// console.log(null > 0)
// console.log(null == 0) // equality check and comparison both are different things in js
// console.log(null >= 0) // js convert null to 0 if we use '>' or '<'

// console.log(undefined >= 0) // for undefined you'll always get ans false

console.log("2" == 2) // true
console.log("2" === 2) // triple check also checks data type if data type not matched then it won't let to convert data types